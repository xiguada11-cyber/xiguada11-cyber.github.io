name: Refresh Exhibition Data Status

on:
  schedule:
    # Beijing 08:00 / 12:00 / 16:00 / 18:00 / 20:00
    # UTC     00:00 / 04:00 / 08:00 / 10:00 / 12:00
    - cron: '0 0,4,8,10,12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Validate JSON and refresh data status
        run: node scripts/refresh-data.mjs
      - name: Commit refreshed status
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: Update data refresh status
          file_pattern: data/update-status.json
